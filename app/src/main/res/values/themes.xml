<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base application theme for the Activity window (status bar, splash background, etc.).
        Compose owns all in-content theming via BehaviorEngineTheme; this XML theme only needs
        to describe the window itself, and stays dark to match the app's dark-only design.
    -->
    <style name="Theme.BehaviorEngine" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>
</resources>
